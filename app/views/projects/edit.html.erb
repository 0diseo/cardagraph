<h1>Editing project</h1>

<%= render "form", project: @project, url: organization_project_path(@organization, @project) %>

<br>

<div>
  <%= link_to "Show this project", organization_project_path(@organization, @project) %> |
  <%= link_to "Back to projects", organization_projects_path %>
</div>
